server:
  port: ${PORT:5000}

spring:
  application:
    name: traffic-fine-system
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb+srv://sanjilraj2_db_user:EJj23DzngVtpOq8y@cluster0.yspoddj.mongodb.net/traffic_fines?appName=Cluster0}
  jackson:
    default-property-inclusion: non_null

app:
  jwt:
    secret: ${JWT_SECRET:change-this-super-secret-key-in-production-please}
    expiration-ms: ${JWT_EXPIRE_MS:604800000}   # 7 days, equivalent of JWT_EXPIRE=7d
  cors:
    allowed-origins: ${CORS_ORIGIN:*}
  rate-limit:
    window-minutes: 15
    max-requests: 100
  seed:
    enabled: ${SEED_ON_STARTUP:true}
  frontend:
    build-path: ${FRONTEND_BUILD_PATH:../frontend/build}
  sms:
    notifylk:
      user-id: ${NOTIFYLK_USER_ID:32234}
      api-key: ${NOTIFYLK_API_KEY:meT29d0ewxHKXixWaTTK}
      sender-id: ${NOTIFYLK_SENDER_ID:NotifyDEMO}

logging:
  level:
    root: INFO
    com.police.trafficfine: DEBUG